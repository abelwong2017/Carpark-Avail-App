import React from "react";
import PropTypes from "prop-types";

import Table from "../components/Table/Table";

const test = (props) => {
    return (
        <div>
            <Table></Table>
        </div>
    );
};

test.propTypes = {};

export default test;
